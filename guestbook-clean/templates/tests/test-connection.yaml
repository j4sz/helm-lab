apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "guestbook-clean.fullname" . }}-test-connection"
  labels:
    {{- include "guestbook-clean.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "guestbook-clean.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
